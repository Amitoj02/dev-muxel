import { ShortcutsSheet } from 'dev-muxel'

/*
 * Its own `Overlay`, narrow variant. A static sheet — the chord table is the
 * component, so there is nothing to vary.
 */
export const Default = (): React.JSX.Element => <ShortcutsSheet />
