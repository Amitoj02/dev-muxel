import { NotesPanel } from 'dev-muxel'

/*
 * Its own `Overlay`, so one full-bleed card. Two seeded notes, one edited
 * minutes ago and one hours ago, which is what the relative timestamps read.
 */
export const Default = (): React.JSX.Element => <NotesPanel />
